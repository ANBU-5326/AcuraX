// Redundant route group layout. Main layout is managed in root and dashboard folder.
export default function RedundantLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
